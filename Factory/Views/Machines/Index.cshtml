@{
  Layout = "_Layout";
}
@using Factory.Models;
<div class="container">
  <div class="jumbotron">
    <h3>Machines</h3>
  </div>
  @if(Model.Count == 0)
  {
    <h6>There are no Machines at this time.</h6>
  }
  <ol>
    @foreach(Machine machine in Model)
    {
      <li>@Html.ActionLink(machine.Name,"Details", new {id = machine.MachineId})</li>
    }
  </ol>
  <button class="btn btn-outline-dark" onclick="window.location.href='/Machines/Create/'">Create a Machine</button>
  <p>@Html.ActionLink("Home","Index","Home")</p>



</div>